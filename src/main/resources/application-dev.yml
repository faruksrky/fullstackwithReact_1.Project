server:
  error:
    include-binding-errors: always
    include-message: always

spring:
  datasource:
    url: jdbc:postgresql://aa1bu5m5gcp0snb.cl6tppex5yii.eu-central-1.rds.amazonaws.com:5432/reactdb
    username: faruksrky
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
