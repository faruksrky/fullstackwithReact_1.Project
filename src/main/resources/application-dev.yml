server:
  error:
    include-binding-errors: always
    include-message: always

spring:
  datasource:
    url: jdbc:postgresql://aaxjld90663wkq.cfjqoi9gvwx6.eu-west-1.rds.amazonaws.com:5432/reactdb
    username: faruksrky
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
